server:
    port: 8080
spring:
    profiles:
      active: development
    datasource:
      url: jdbc:mysql://localhost:13306/staff_mgmt
      username: root
      password: admin
    flyway:
      enabled: true

    jpa:
      hibernate:
        ddl-auto: none
